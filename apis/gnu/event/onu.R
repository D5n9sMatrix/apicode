#!/usr/bin/r

# or convenience, both row operations are done in one step. The result is
# Note that the last two manipulations did not affect the first column (the 
# second row has a zero there), so our previous effort there has not been 
# undermined. Finally we clean up the third column. Begin by multiplying row 3 
# by − 17 to obtain

row1 <- R1 + R2 + R3 / R4 ^ R5 

